version: v1
configuration:
  defaults:
    branch:
      validations:
        BUILD: {}
        TESTS:
          tests: {}
        GITHUB.RELEASE: {}
      promotions:
        BRONZE:
          validations:
            - BUILD
            - TESTS
        RELEASE:
          promotions:
            - BRONZE
          validations:
            - GITHUB.RELEASE
