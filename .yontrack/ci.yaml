version: v1
configuration:
  defaults:
    branch:
      validations:
        BUILD: {}
        TESTS:
          tests: {}
      promotions:
        BRONZE:
          validations:
            - BUILD
            - TESTS
