version: v1
configuration:
  defaults:
    branch:
      validations:
        BUILD: {}
        TESTS:
          tests: {}
      promotion:
        BRONZE:
          validations:
            - BUILD
            - TESTS
